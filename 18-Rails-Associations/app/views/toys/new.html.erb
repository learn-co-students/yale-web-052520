<%= form_for @toy do |f| %>

<%= f.label :name %>
<%= f.text_field :name %>

<%= f.collection_select :dog_id, @dogs , :id, :name %>

    <%= f.fields_for @dog do |df| %>
        <%= df.label "Dog Name" %>
        <%= df.text_field :name%>
    <% end %>

<%= f.submit%>
<% end %>